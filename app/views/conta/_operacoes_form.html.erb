<div class="flex flex-col items-center h-[400px] w-[300px] bg-gradient-to-r from-black to-primary rounded-xl">
    <%= form_with model: @contum do |f|%>

        <%=f.label :decimal ,"Valor da operação"%>
        <%=f.%>

        <%=f.submit "Sacar"%>
    <%end%>
</div>