  <header class= "flex items-center flex-row bg-black">
    <h1 class="pl-10 font-bold text-4xl p-10">Bem vindo, <%= current_user.nome%></h1>
    <span class="ml-auto">Você está acessando a conta <%=@contum.nome%></span>
  </header>